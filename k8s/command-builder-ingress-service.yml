apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-service
  annotations:
    kubernetes.io/ingress.class: 'nginx'
spec:
  rules:
    - http:
        paths:
          - pathType: Prefix
            path: /
            backend:
              service:
                name: command-builder-frontend-cluster-ip-service
                port:
                  number: 3000
          - pathType: Prefix
            path: /api/
            backend:
              service:
                name: command-builder-backend-cluster-ip-service
                port:
                  number: 8080
